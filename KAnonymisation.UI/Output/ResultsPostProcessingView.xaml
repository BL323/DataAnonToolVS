<UserControl x:Class="KAnonymisation.UI.Output.ResultsPostProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid Background="DeepPink">
        <StackPanel Orientation="Vertical">

            <StackPanel Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Results}" DisplayMemberPath="AnonTitle" SelectedItem="{Binding PostProcessingVm.SelectedResult}" Width="200" Margin="5"/>
                <Button Content="Extract Meterics" Command="{Binding PostProcessingVm.ExtractMetericsCommand}" Width="100" Height="25" Margin="5"/>
            </StackPanel>

            <TextBox Text="{Binding PostProcessingVm.SelectedResult.ExtractedMeterics}" Width="310" HorizontalAlignment="Left" Margin="5" Height="100"/>
            
        </StackPanel>
    </Grid>
</UserControl>
