<UserControl x:Class="AnonTool.UI.MenuBars.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
            <Button Content="Import" Height="25" Width="75" Command="{Binding ImportDataCommand}" />
            <Button Content="Export" Height="25" Width="75" Command="{Binding ExportDataCommand}" Margin="10, 0, 10, 0"/>
        </StackPanel>
    </Grid>
</UserControl>
