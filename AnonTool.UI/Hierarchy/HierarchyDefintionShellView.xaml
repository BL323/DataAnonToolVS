<UserControl x:Class="AnonTool.UI.Hierarchy.HierarchyDefintionShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:HierarchyViews="clr-namespace:AnonTool.UI.Hierarchy"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <HierarchyViews:HierarchyDefinitionOptionsView Grid.Row="1" Grid.Column="0" DataContext="{Binding HierarchyDefintionOptionsVm}" />
        <DataGrid ItemsSource="{Binding HierarchyDefintions}" AutoGenerateColumns="True" Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
